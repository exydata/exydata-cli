# download voltmetrix-cli from github and install it to /usr/local/bin

# download voltmetrix-cli

wget https://github.com/volmetrixdotcom/cli/releases/download/0.0.1/voltmetrix -O /usr/local/bin/voltmetrix

# make it executable

chmod +x /usr/local/bin/voltmetrix

# done

echo "voltmetrix-cli installed to /usr/local/bin"
